backend: nsight
enabled: true
worker_groups: [ActorGroup, RolloutGroup, EnvGroup]
options:
  t: cuda,cudnn,cublas,nvtx,osrt
  sample: process-tree
  cpuctxsw: process-tree
  cudabacktrace: all
  osrt-threshold: 1000
flags: []
