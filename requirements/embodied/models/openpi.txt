# Minimal OpenPI runtime dependencies for RLinf OpenPI wrapper.
# Keep these pins to avoid known compatibility breakages:
# - openpi pins jax==0.5.3; orbax-checkpoint==0.11.13 imports
#   jax.experimental.layout.DeviceLocalLayout, which was removed in jax>=0.7.0.
jax[cuda12]==0.5.3
orbax-checkpoint==0.11.13
tyro==1.0.13
