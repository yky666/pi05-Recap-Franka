defaults:
  - env/isaaclab_stack_cube@env.train
  - env/isaaclab_stack_cube@env.eval
  - model/gr00t@actor.model
  - hybrid_engines/fsdp@actor.fsdp_config
  - weight_syncer/patch_syncer@weight_syncer
  - override hydra/job_logging: stdout

hydra:
  run:
    dir: .
  output_subdir: null
  searchpath:
    - file://${oc.env:REPO_PATH}/examples/embodiment/config

cluster:
  num_nodes: 1
  component_placement:
      env: 0-1
      rollout:
        node_group: "rollout"
        placement: 0-1
      actor: 0-1

  node_groups:
    - label: "rollout"
      node_ranks: all
      env_configs:
        - node_ranks: all
          env_vars:
            # The gr00t model contains numerous numpy operations, whose performance is sensitive to the OMP_NUM_THREADS environment variable.
            # By default, this is set to 1 by Ray. Consider increasing it to improve performance.
            # Recommended to increase to number of CPU cores (use lscpu to check) divided by rollout world size.
            # Do not set this globally in the shell script which will increase the number of threads for all processes,
            # because PyTorch performance will be degraded, contrary to numpy.
            - OMP_NUM_THREADS: 32

runner:
  task_type: embodied
  logger:
    log_path: /workspace/results/isaaclab_ppo_gr00t
    project_name: rlinf
    experiment_name: "ci-test"
    logger_backends: ["tensorboard"] # wandb, swanlab

  max_epochs: 1
  max_steps: -1

  only_eval: False
  val_check_interval: -1
  save_interval: -1

algorithm:
  normalize_advantages: True
  kl_penalty: kl  # how to estimate kl divergence: kl or kl_penalty
  group_size: 1
  reward_coef: 1.0

  reward_type: chunk_level
  logprob_type: chunk_level
  entropy_type: chunk_level

  update_epoch: 1
  adv_type: gae
  loss_type: actor_critic
  loss_agg_func: "token-mean" 
  kl_beta: 0.0
  entropy_bonus: 0
  clip_ratio_high: 0.2
  clip_ratio_low: 0.2
  clip_ratio_c: 3.0
  value_clip: 0.2
  huber_delta: 10.0

  gamma: 0.99
  gae_lambda: 0.95

  filter_rewards: False
  rewards_lower_bound: 0.1
  rewards_upper_bound: 0.9
env:
  group_name: "EnvGroup"

  # Override the default values in env/isaaclab_stack_cube
  train:
    rollout_epoch: 1
    total_num_envs: 4
    max_episode_steps: 16
    max_steps_per_rollout_epoch: 16
    use_fixed_reset_state_ids: True

  eval:
    rollout_epoch: 1 # set eval.rollout_epoch > 0 when enable runner.only_eval or runner.val_check_interval > 0
    total_num_envs: 1
    max_episode_steps: 16
    max_steps_per_rollout_epoch: 16
    auto_reset: True
    ignore_terminations: True
    use_fixed_reset_state_ids: True
    video_cfg:
      save_video: True
      info_on_video: True
      video_base_dir: ${runner.logger.log_path}/video/eval

rollout:
  group_name: "RolloutGroup"
  backend: "huggingface"
  enable_offload: True
  pipeline_stage_num: 1

  model:
    model_path: "/workspace/dataset/RLinf-Gr00t-SFT-Stack-cube"
    precision: ${actor.model.precision}

actor:
  group_name: "ActorGroup"
  training_backend: "fsdp"
  micro_batch_size: 1
  global_batch_size: 4
  seed: 1234
  enable_offload: False

  # Override the default values in model/gr00t
  model:
    model_path: "/workspace/dataset/RLinf-Gr00t-SFT-Stack-cube"
    add_value_head: True
    embodiment_tag: "isaaclab_franka"
    obs_converter_type: "isaaclab_stack_cube"
    num_action_chunks: 16

  optim:
    lr: 2.0e-5
    value_lr: 2.0e-5
    adam_beta1: 0.9
    adam_beta2: 0.95
    adam_eps: 1.0e-08
    weight_decay: 0.01
    clip_grad: 1.0
    critic_warmup_steps: 0
  
  # Override the default values in hybrid_engines/fsdp
  fsdp_config:
    strategy: "fsdp"
    gradient_checkpointing: True
    mixed_precision:
      param_dtype: ${actor.model.precision}
      reduce_dtype: ${actor.model.precision}
      buffer_dtype: ${actor.model.precision}

reward:
  use_reward_model: False

critic:
  use_critic_model: False